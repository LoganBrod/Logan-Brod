# context-server

Placeholder — built in Phase 3.

An MCP server (TypeScript, official SDK, stdio transport) that exposes
`memory/` as structured tools: `search_context`, `log_decision`,
`get_business`, `list_open_questions`, `add_open_question`.

This file is replaced with real docs once the server exists.
