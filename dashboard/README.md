# Dashboard

Placeholder. This will become the projector display.

Nothing is built here yet — deliberately. The dashboard should render what's
already in `memory/`, so the memory format needs to settle first. Building the
display before the data it displays would lock in a schema chosen for looks.

When it does get built, the open question is whether it reads the memory files
directly or goes through the MCP context server. Logged as an open question
when we get there.
