/** @type {import('next').NextConfig} */
// Listing images come from arbitrary merchant domains, so they render through
// plain <img> rather than next/image — there is no host list to configure.
const nextConfig = {};

module.exports = nextConfig;
